<div class="container">
  <h1>Welcome to <strong>MUGLANDIA</strong></h1>
  <h2>Rent Your Favorite Mugs</h2>

  <div class="menu">
    <h2>Home
      <ul>
        <li><%= link_to "All mugs", mugs_path  %></li>
      </ul>
    </h2>
  </div>

  <div class="intro">
    <p>Do you love mugs as much as we do?</p>
    <p>Here you can rent your favorite mugs from other mug lovers.</p>
  </div>

  <div class="card-container">
    <h3>Available Mugs</h3>
    <div class="cards">
      <% @mugs.each do |mug| %>
          <div class="flip-card">
              <div class="flip-card-inner">
                <div class="flip-card-front">
                    <img src="<%= mug.picture %>" alt="<%= mug.description %>">
                    <h4 style="margin-top: 10px">localisation: <%= mug.localisation %></h4>
                </div>

                <div class="flip-card-back">
                  <%=link_to mug_path(mug) do%>
                    <img src="<%= mug.picture %>" alt="<%= mug.description %>">
                    <h3><%= mug.description %></h3>
                    <p>Size: <%= mug.size %></p>
                    <p>Price: <%= mug.price %></p>
                    <p>Color: <%= mug.color %></p>
                  <% end  %>
                </div>
              </div>
          </div>
      <% end %>
    </div>
  </div>

  <%# commentaires %>

<%# end commment %>

</div>
